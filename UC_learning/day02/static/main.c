#if 0
//调用模块
//#include"calc.h"
//#include"show.h"
#include"math.h"

int main(void){
    int a = 123,b = 456;
    int res = add(a,b);
    show(a,'+',b,res);

    res = sub(a,b);
    show(a,'-',b,res);
    return 0;
}
#endif

